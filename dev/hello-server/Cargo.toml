[package]
name = "hello-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
leptos = { version = "0.7", features = ["ssr"] }
leptos_axum = "0.7"
tokio = { version = "1", features = ["full"] }
dotenvy = "0.15"
tower = "0.5"
tower-http = { version = "0.6", features = ["fs"] }
