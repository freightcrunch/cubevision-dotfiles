apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cicd
# Tekton resources mirroring ams-bol-system's GitHub Actions workflows.
# Secrets (ams-bol-ssh, ams-bol-secrets) are created out-of-band — see SECRETS.md.
resources:
  - build-tasks.yaml
  - ssh-deploy-task.yaml
  - deploy-pipeline.yaml
  - ops-db-tasks.yaml
  - ops-maint-tasks.yaml
